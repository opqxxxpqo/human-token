{
  "name": "human-token",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "clean": "powershell -NoProfile -ExecutionPolicy Bypass -File clean.ps1",
    "clean:all": "powershell -NoProfile -ExecutionPolicy Bypass -File clean.ps1 -All"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  }
}
